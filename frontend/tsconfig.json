{
	// define parent tsconfig
	"extends": "../tsconfig.json",
	"compilerOptions": {
		// transpilation settings
		"target": "ESNext",
		"lib": ["DOM"],

		// filesystem
		"rootDirs": ["./", "../"],
		"baseUrl": ".",
		"paths": {}
	}
}
